openapi: 3.0.3
info:
  title: Cosmosh Local API
  version: 0.1.0
  description: |
    Internal local API used by Cosmosh. In Electron runtime, requests must be proxied by main process.
servers:
  - url: http://127.0.0.1:{port}
    variables:
      port:
        default: '3000'
security:
  - CosmoshInternalToken: []
paths:
  /api/health:
    get:
      tags:
        - system
      summary: Backend liveness check.
      operationId: health
      security: []
      responses:
        '200':
          description: Backend service is alive.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /api/v1/test/ping:
    get:
      tags:
        - test
      summary: Connectivity smoke test for renderer -> main -> backend chain.
      operationId: testPing
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      responses:
        '200':
          description: Successful ping response.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TestPingSuccess'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/runtime/active-connections:
    get:
      tags:
        - runtime
      summary: Get active SSH and SFTP connection counts.
      operationId: runtimeGetActiveConnections
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      responses:
        '200':
          description: Active connection counts returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeActiveConnectionsGetSuccess'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
    delete:
      tags:
        - runtime
      summary: Close all active SSH and SFTP connections.
      operationId: runtimeCloseActiveConnections
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      responses:
        '200':
          description: Active SSH and SFTP connections closed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeActiveConnectionsCloseSuccess'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/settings:
    get:
      tags:
        - settings
      summary: Get application settings.
      operationId: settingsGet
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      responses:
        '200':
          description: Settings fetched.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SettingsGetSuccess'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
    put:
      tags:
        - settings
      summary: Update application settings.
      operationId: settingsUpdate
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SettingsUpdateRequest'
      responses:
        '200':
          description: Settings updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SettingsUpdateSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/audit/events:
    get:
      tags:
        - audit
      summary: List audit events.
      operationId: auditListEvents
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: page
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
        - name: pageSize
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
        - name: startAt
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: endAt
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: category
          in: query
          required: false
          schema:
            type: string
        - name: outcome
          in: query
          required: false
          schema:
            type: string
        - name: entityType
          in: query
          required: false
          schema:
            type: string
        - name: entityId
          in: query
          required: false
          schema:
            type: string
        - name: keyword
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Audit events listed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditEventListSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/audit/events/{eventId}:
    get:
      tags:
        - audit
      summary: Get one audit event detail.
      operationId: auditGetEventById
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: eventId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Audit event detail fetched.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditEventDetailSuccess'
        '404':
          description: Audit event not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/ssh/servers:
    get:
      tags:
        - ssh
      summary: List SSH servers.
      operationId: sshListServers
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      responses:
        '200':
          description: SSH servers listed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SshServerListSuccess'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
    post:
      tags:
        - ssh
      summary: Create SSH server configuration.
      operationId: sshCreateServer
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SshServerCreateRequest'
      responses:
        '200':
          description: SSH server created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SshServerCreateSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: Conflict with existing server.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/ssh/servers/{serverId}:
    put:
      tags:
        - ssh
      summary: Update SSH server configuration.
      operationId: sshUpdateServer
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: serverId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SshServerCreateRequest'
      responses:
        '200':
          description: SSH server updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SshServerUpdateSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: SSH server not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: Conflict with existing server.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
    delete:
      tags:
        - ssh
      summary: Delete SSH server configuration.
      operationId: sshDeleteServer
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: serverId
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: SSH server deleted.
        '404':
          description: SSH server not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/ssh/servers/{serverId}/credentials:
    get:
      tags:
        - ssh
      summary: Get SSH server credentials for editor form prefill.
      operationId: sshGetServerCredentials
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: serverId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SSH server credentials fetched.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SshServerCredentialsSuccess'
        '404':
          description: SSH server not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/ssh/keychains:
    get:
      tags:
        - ssh
      summary: List SSH keychains.
      operationId: sshListKeychains
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      responses:
        '200':
          description: SSH keychains listed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SshKeychainListSuccess'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
    post:
      tags:
        - ssh
      summary: Create SSH keychain.
      operationId: sshCreateKeychain
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SshKeychainCreateRequest'
      responses:
        '200':
          description: SSH keychain created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SshKeychainCreateSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: Conflict with existing keychain.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/ssh/keychains/{keychainId}:
    put:
      tags:
        - ssh
      summary: Update SSH keychain.
      operationId: sshUpdateKeychain
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: keychainId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SshKeychainUpdateRequest'
      responses:
        '200':
          description: SSH keychain updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SshKeychainUpdateSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: SSH keychain not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
    delete:
      tags:
        - ssh
      summary: Delete SSH keychain.
      operationId: sshDeleteKeychain
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: keychainId
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: SSH keychain deleted.
        '404':
          description: SSH keychain not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: SSH keychain is in use.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/ssh/keychains/{keychainId}/credentials:
    get:
      tags:
        - ssh
      summary: Get SSH keychain credentials for editor form prefill.
      operationId: sshGetKeychainCredentials
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: keychainId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SSH keychain credentials fetched.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SshKeychainCredentialsSuccess'
        '404':
          description: SSH keychain not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/ssh/folders:
    get:
      tags:
        - ssh
      summary: List SSH folders.
      operationId: sshListFolders
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      responses:
        '200':
          description: SSH folders listed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SshFolderListSuccess'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
    post:
      tags:
        - ssh
      summary: Create SSH folder.
      operationId: sshCreateFolder
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SshFolderCreateRequest'
      responses:
        '200':
          description: SSH folder created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SshFolderCreateSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: Folder already exists.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/ssh/folders/{folderId}:
    put:
      tags:
        - ssh
      summary: Update SSH folder.
      operationId: sshUpdateFolder
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: folderId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SshFolderUpdateRequest'
      responses:
        '200':
          description: SSH folder updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SshFolderUpdateSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: SSH folder not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: Folder already exists.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
    delete:
      tags:
        - ssh
      summary: Delete SSH folder.
      operationId: sshDeleteFolder
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: folderId
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: SSH folder deleted.
        '404':
          description: SSH folder not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/ssh/tags:
    get:
      tags:
        - ssh
      summary: List SSH tags.
      operationId: sshListTags
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      responses:
        '200':
          description: SSH tags listed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SshTagListSuccess'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
    post:
      tags:
        - ssh
      summary: Create SSH tag.
      operationId: sshCreateTag
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SshTagCreateRequest'
      responses:
        '200':
          description: SSH tag created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SshTagCreateSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: Tag already exists.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/ssh/sessions:
    post:
      tags:
        - ssh
      summary: Create a live SSH terminal session.
      operationId: sshCreateSession
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SshSessionCreateRequest'
      responses:
        '200':
          description: SSH session is ready for websocket streaming.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SshSessionCreateSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Server not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: Host fingerprint is not trusted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SshHostVerificationRequired'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/ssh/sessions/{sessionId}:
    delete:
      tags:
        - ssh
      summary: Close a live SSH terminal session.
      operationId: sshCloseSession
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: Session closed.
        '404':
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/ssh/trusted-host-keys:
    post:
      tags:
        - ssh
      summary: Trust a host fingerprint for a server.
      operationId: sshTrustFingerprint
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SshTrustFingerprintRequest'
      responses:
        '200':
          description: Host fingerprint has been trusted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SshTrustFingerprintSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Server not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions:
    post:
      tags:
        - sftp
      summary: Create an SFTP file-system session.
      operationId: sftpCreateSession
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SftpSessionCreateRequest'
      responses:
        '200':
          description: SFTP session is ready for file-system operations.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpSessionCreateSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Server not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: Host fingerprint is not trusted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SshHostVerificationRequired'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}/entries:
    get:
      tags:
        - sftp
      summary: List entries in one remote SFTP directory.
      operationId: sftpListDirectory
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
        - name: path
          in: query
          required: false
          schema:
            type: string
            default: .
      responses:
        '200':
          description: Directory entries listed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpDirectoryListSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Session or directory not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}/entries/details:
    post:
      tags:
        - sftp
      summary: Get detailed metadata for selected remote SFTP entries.
      operationId: sftpGetEntryDetails
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SftpEntryDetailsRequest'
      responses:
        '200':
          description: Detailed entry metadata fetched.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpEntryDetailsSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}/file:
    get:
      tags:
        - sftp
      summary: Read one remote SFTP file for preview.
      operationId: sftpReadFile
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
        - name: path
          in: query
          required: true
          schema:
            type: string
        - name: maxBytes
          in: query
          required: false
          schema:
            type: integer
            minimum: 1024
            maximum: 16777216
            default: 262144
      responses:
        '200':
          description: File content read.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpReadFileSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Session or file not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
    post:
      tags:
        - sftp
      summary: Write UTF-8 text content to one remote SFTP file.
      operationId: sftpWriteFile
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SftpWriteFileRequest'
      responses:
        '200':
          description: File written.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpOperationSuccess'
        '400':
          description: Validation failed or write failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: Remote file changed after the preview was opened.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Session or file not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}/download:
    post:
      tags:
        - sftp
      summary: Download one remote SFTP file to a local path.
      operationId: sftpDownloadFile
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SftpDownloadFileRequest'
      responses:
        '200':
          description: File downloaded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpDownloadFileSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Session or file not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}/upload:
    post:
      tags:
        - sftp
      summary: Upload one staged local file to a remote path.
      operationId: sftpUploadFile
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SftpUploadFileRequest'
      responses:
        '200':
          description: File uploaded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpOperationSuccess'
        '400':
          description: Validation failed or upload failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: The remote target already exists or changed after it was opened.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Session or file not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/transfers/{transferId}:
    get:
      tags:
        - sftp
      summary: Read the latest progress snapshot for one active or recently completed SFTP transfer.
      operationId: sftpGetTransferProgress
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: transferId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Transfer progress snapshot.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpTransferProgressSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Transfer progress record not found or expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}/directories:
    post:
      tags:
        - sftp
      summary: Create one remote SFTP directory.
      operationId: sftpCreateDirectory
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SftpCreateDirectoryRequest'
      responses:
        '200':
          description: Directory created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpOperationSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}/files:
    post:
      tags:
        - sftp
      summary: Create one empty remote SFTP file.
      operationId: sftpCreateFile
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SftpCreateFileRequest'
      responses:
        '200':
          description: File created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpOperationSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}/rename:
    post:
      tags:
        - sftp
      summary: Rename or move one remote SFTP entry.
      operationId: sftpRenameEntry
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SftpRenameRequest'
      responses:
        '200':
          description: Entry renamed or moved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpOperationSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Session or entry not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}/copy:
    post:
      tags:
        - sftp
      summary: Copy one remote SFTP entry.
      operationId: sftpCopyEntry
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SftpCopyRequest'
      responses:
        '200':
          description: Entry copied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpOperationSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Session or entry not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}/entries/delete:
    post:
      tags:
        - sftp
      summary: Delete one remote SFTP entry.
      operationId: sftpDeleteEntry
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SftpDeleteRequest'
      responses:
        '200':
          description: Entry deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpOperationSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Session or entry not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}/batch:
    post:
      tags:
        - sftp
      summary: Run one ordered batch operation across remote SFTP entries.
      operationId: sftpBatchOperation
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SftpBatchOperationRequest'
      responses:
        '200':
          description: Batch operation completed. The summary reports failed and skipped entries when fail-fast stops execution.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpBatchOperationSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Session or entry not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}/tasks:
    post:
      tags:
        - sftp
      summary: Submit one asynchronous SFTP file-system task.
      operationId: sftpStartTask
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SftpTaskStartRequest'
      responses:
        '202':
          description: Task accepted for bounded asynchronous execution.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpTaskAcceptedSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
    get:
      tags:
        - sftp
      summary: List retained asynchronous tasks for one SFTP session.
      operationId: sftpListTasks
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Retained tasks returned in creation order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpTaskListSuccess'
        '404':
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}/tasks/{taskId}:
    get:
      tags:
        - sftp
      summary: Read one retained asynchronous SFTP task.
      operationId: sftpGetTask
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
        - name: taskId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Task status returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpTaskStatusSuccess'
        '400':
          description: Task identifier validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Session or task not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}/archive-capabilities:
    get:
      tags:
        - sftp
      summary: Probe archive formats supported by the remote SFTP session.
      operationId: sftpGetArchiveCapabilities
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Archive capabilities were probed or returned from the session cache.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpArchiveCapabilitiesSuccess'
        '404':
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: The session scheduler cannot grant immediate exclusive probe ownership.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}/archive-operations:
    post:
      tags:
        - sftp
      summary: Start one controlled remote archive operation.
      operationId: sftpStartArchiveOperation
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SftpArchiveOperationRequest'
      responses:
        '202':
          description: Archive operation accepted and started asynchronously.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpArchiveOperationAcceptedSuccess'
        '400':
          description: Validation failed or the requested archive format is unsupported.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Session or source entry not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: Another archive operation is active or the target already exists.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}/archive-operations/{operationId}:
    get:
      tags:
        - sftp
      summary: Read the current state of an archive operation.
      operationId: sftpGetArchiveOperation
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
        - name: operationId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Archive operation status returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpArchiveOperationStatusSuccess'
        '404':
          description: Session or archive operation not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
    delete:
      tags:
        - sftp
      summary: Request cancellation of an archive operation.
      operationId: sftpCancelArchiveOperation
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
        - name: operationId
          in: path
          required: true
          schema:
            type: string
      responses:
        '202':
          description: Cancellation requested. Poll the operation until it reaches a terminal state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpArchiveOperationStatusSuccess'
        '404':
          description: Session or archive operation not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: The operation could not be cancelled.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}/archive-operations/{operationId}/conflict-resolution:
    post:
      tags:
        - sftp
      summary: Resolve all pending destination conflicts for one archive operation.
      operationId: sftpResolveArchiveConflict
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
        - name: operationId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SftpArchiveConflictResolutionRequest'
      responses:
        '200':
          description: Conflict resolution accepted and the operation resumed or was cancelled.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SftpArchiveOperationStatusSuccess'
        '400':
          description: Validation failed or the operation is not waiting for a conflict decision.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Session or archive operation not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/sftp/sessions/{sessionId}:
    delete:
      tags:
        - sftp
      summary: Close an SFTP file-system session.
      operationId: sftpCloseSession
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: Session closed.
        '404':
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/local-terminals/profiles:
    get:
      tags:
        - local-terminal
      summary: List local terminal profiles available on host.
      operationId: localTerminalListProfiles
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      responses:
        '200':
          description: Local terminal profiles listed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LocalTerminalProfileListSuccess'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/local-terminals/sessions:
    post:
      tags:
        - local-terminal
      summary: Create a live local terminal session.
      operationId: localTerminalCreateSession
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocalTerminalSessionCreateRequest'
      responses:
        '200':
          description: Local terminal session is ready for websocket streaming.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LocalTerminalSessionCreateSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Profile not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/local-terminals/sessions/{sessionId}:
    delete:
      tags:
        - local-terminal
      summary: Close a live local terminal session.
      operationId: localTerminalCloseSession
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: Session closed.
        '404':
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/port-forwards/rules:
    get:
      tags:
        - port-forward
      summary: List persisted SSH port-forwarding rules.
      operationId: portForwardListRules
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      responses:
        '200':
          description: Port-forwarding rules listed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PortForwardRuleListSuccess'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
    post:
      tags:
        - port-forward
      summary: Create one persisted SSH port-forwarding rule.
      operationId: portForwardCreateRule
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PortForwardRuleCreateRequest'
      responses:
        '200':
          description: Port-forwarding rule created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PortForwardRuleCreateSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Server not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/port-forwards/rules/{ruleId}:
    put:
      tags:
        - port-forward
      summary: Update one stopped SSH port-forwarding rule.
      operationId: portForwardUpdateRule
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: ruleId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PortForwardRuleUpdateRequest'
      responses:
        '200':
          description: Port-forwarding rule updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PortForwardRuleUpdateSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Rule or server not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: Rule is active.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
    delete:
      tags:
        - port-forward
      summary: Delete one stopped SSH port-forwarding rule.
      operationId: portForwardDeleteRule
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: ruleId
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: Port-forwarding rule deleted.
        '404':
          description: Rule not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: Rule is active.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/port-forwards/rules/{ruleId}/start:
    post:
      tags:
        - port-forward
      summary: Start one persisted SSH port-forwarding rule.
      operationId: portForwardStartRule
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: ruleId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PortForwardRuleStartRequest'
      responses:
        '200':
          description: Port-forwarding rule started.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PortForwardRuleStartSuccess'
        '400':
          description: Start failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Rule or server not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: Rule is already active or host fingerprint is not trusted.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/ApiError'
                  - $ref: '#/components/schemas/SshHostVerificationRequired'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/port-forwards/rules/{ruleId}/stop:
    post:
      tags:
        - port-forward
      summary: Stop one active SSH port-forwarding rule.
      operationId: portForwardStopRule
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: ruleId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Port-forwarding rule stopped.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PortForwardRuleStopSuccess'
        '404':
          description: Rule not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/mcp/status:
    get:
      tags:
        - mcp
      summary: Get MCP runtime status for the management UI.
      operationId: mcpGetStatus
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      responses:
        '200':
          description: MCP status returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/McpStatusGetSuccess'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/mcp/pairing-token:
    post:
      tags:
        - mcp
      summary: Create or rotate the MCP pairing token.
      operationId: mcpRotatePairingToken
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      responses:
        '200':
          description: Pairing token created; plaintext is only returned once.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/McpPairingTokenRotateSuccess'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
    delete:
      tags:
        - mcp
      summary: Revoke the active MCP pairing token.
      operationId: mcpRevokePairingToken
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      responses:
        '204':
          description: Pairing token revoked.
        '404':
          description: No active pairing token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/mcp/clients:
    get:
      tags:
        - mcp
      summary: List active MCP client sessions.
      operationId: mcpListClients
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      responses:
        '200':
          description: MCP client sessions listed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/McpClientListSuccess'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/mcp/connections:
    get:
      tags:
        - mcp
      summary: List active agent-opened MCP SSH connections.
      operationId: mcpListConnections
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      responses:
        '200':
          description: MCP SSH connections listed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/McpConnectionListSuccess'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/mcp/connections/{connectionId}:
    delete:
      tags:
        - mcp
      summary: Close one agent-opened MCP SSH connection.
      operationId: mcpCloseConnection
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: connectionId
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: MCP SSH connection closed.
        '404':
          description: Connection not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/mcp/approvals:
    get:
      tags:
        - mcp
      summary: List pending MCP authorization requests.
      operationId: mcpListApprovals
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      responses:
        '200':
          description: Pending approvals listed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/McpApprovalListSuccess'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/mcp/approvals/{approvalId}/decision:
    post:
      tags:
        - mcp
      summary: Resolve one pending MCP authorization request.
      operationId: mcpResolveApproval
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
        - name: approvalId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/McpApprovalDecisionRequest'
      responses:
        '200':
          description: Approval resolved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/McpApprovalResolveSuccess'
        '400':
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '404':
          description: Approval not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '409':
          description: Approval already resolved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
  /api/v1/mcp/events-channel:
    post:
      tags:
        - mcp
      summary: Create a WebSocket channel delivering MCP runtime events to the renderer.
      operationId: mcpCreateEventsChannel
      parameters:
        - $ref: '#/components/parameters/LocaleHeader'
      responses:
        '200':
          description: Events channel created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/McpEventsChannelCreateSuccess'
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
components:
  securitySchemes:
    CosmoshInternalToken:
      type: apiKey
      in: header
      name: x-cosmosh-internal-token
  parameters:
    LocaleHeader:
      name: x-cosmosh-locale
      in: header
      required: false
      schema:
        type: string
  schemas:
    HealthResponse:
      type: object
      required:
        - status
        - timestamp
      properties:
        status:
          type: string
          enum: ['ok']
        timestamp:
          type: string
          format: date-time
    ApiMeta:
      type: object
      required:
        - code
        - message
        - requestId
        - timestamp
      properties:
        code:
          type: string
        message:
          type: string
        requestId:
          type: string
        timestamp:
          type: string
          format: date-time
    ApiError:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - code
          properties:
            code:
              type: string
              enum:
                [
                  'COMMON_INTERNAL_SERVER_ERROR',
                  'AUTH_INVALID_TOKEN',
                  'SETTINGS_VALIDATION_FAILED',
                  'SSH_VALIDATION_FAILED',
                  'SSH_SERVER_CONFLICT',
                  'SSH_FOLDER_CONFLICT',
                  'SSH_TAG_CONFLICT',
                  'SSH_KEYCHAIN_CONFLICT',
                  'SSH_NOT_FOUND',
                  'SSH_HOST_UNTRUSTED',
                  'SSH_SESSION_NOT_FOUND',
                  'SSH_KEYCHAIN_IN_USE',
                  'SFTP_SESSION_NOT_FOUND',
                  'SFTP_TRANSFER_NOT_FOUND',
                  'SFTP_TASK_NOT_FOUND',
                  'SFTP_VALIDATION_FAILED',
                  'SFTP_OPERATION_FAILED',
                  'SFTP_UPLOAD_CONFLICT',
                  'SFTP_ARCHIVE_UNSUPPORTED',
                  'SFTP_ARCHIVE_BUSY',
                  'SFTP_ARCHIVE_TARGET_EXISTS',
                  'SFTP_ARCHIVE_UNSAFE_ENTRY',
                  'SFTP_ARCHIVE_OPERATION_NOT_FOUND',
                  'SFTP_ARCHIVE_OPERATION_FAILED',
                  'SFTP_ARCHIVE_TIMEOUT',
                  'SFTP_ARCHIVE_CANCEL_FAILED',
                  'AUDIT_VALIDATION_FAILED',
                  'AUDIT_EVENT_NOT_FOUND',
                  'LOCAL_TERMINAL_VALIDATION_FAILED',
                  'LOCAL_TERMINAL_PROFILE_NOT_FOUND',
                  'PORT_FORWARD_VALIDATION_FAILED',
                  'PORT_FORWARD_RULE_NOT_FOUND',
                  'PORT_FORWARD_RULE_ACTIVE',
                  'PORT_FORWARD_START_FAILED',
                  'PORT_FORWARD_STOP_FAILED',
                  'MCP_DISABLED',
                  'MCP_VALIDATION_FAILED',
                  'MCP_APPROVAL_NOT_FOUND',
                  'MCP_APPROVAL_ALREADY_RESOLVED',
                  'MCP_CONNECTION_NOT_FOUND',
                  'MCP_PAIRING_TOKEN_NOT_FOUND',
                ]
            success:
              type: boolean
              enum: [false]
    SshAuthType:
      type: string
      enum: ['password', 'key', 'both']
    SshKeychainVisibility:
      type: string
      enum: ['hidden', 'shared']
    TestPingData:
      type: object
      required:
        - service
        - mode
        - authenticated
        - capabilities
      properties:
        service:
          type: string
          enum: ['cosmosh-backend']
        mode:
          type: string
          enum: ['electron-main', 'standalone']
        authenticated:
          type: boolean
        capabilities:
          type: array
          items:
            type: string
            enum: ['ssh', 'sftp', 'port-forward']
          minItems: 3
          maxItems: 3
    TestPingSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['TEST_PING_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/TestPingData'
    RuntimeActiveConnectionsData:
      type: object
      required:
        - sshCount
        - sftpCount
        - totalCount
      properties:
        sshCount:
          type: integer
          minimum: 0
        sftpCount:
          type: integer
          minimum: 0
        totalCount:
          type: integer
          minimum: 0
    RuntimeActiveConnectionsGetSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['RUNTIME_ACTIVE_CONNECTIONS_GET_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/RuntimeActiveConnectionsData'
    RuntimeActiveConnectionsCloseSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['RUNTIME_ACTIVE_CONNECTIONS_CLOSE_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/RuntimeActiveConnectionsData'
    SettingsValues:
      type: object
      description: >
        Application settings values. Property definitions, types, defaults,
        constraints, and enum sets are managed by the settings registry at
        packages/api-contract/src/settings-registry.ts.
        This schema is intentionally loose; runtime validation lives in code.
      additionalProperties: true
    SettingsScope:
      type: object
      required:
        - deviceId
      properties:
        accountId:
          type: string
        deviceId:
          type: string
          minLength: 1
          maxLength: 120
    SettingsResource:
      type: object
      required:
        - scope
        - revision
        - updatedAt
        - values
      properties:
        scope:
          $ref: '#/components/schemas/SettingsScope'
        revision:
          type: integer
          minimum: 0
        updatedAt:
          type: string
          format: date-time
        values:
          $ref: '#/components/schemas/SettingsValues'
    SettingsGetData:
      type: object
      required:
        - item
      properties:
        item:
          $ref: '#/components/schemas/SettingsResource'
    SettingsUpdateRequest:
      type: object
      required:
        - values
      properties:
        scope:
          $ref: '#/components/schemas/SettingsScope'
        values:
          $ref: '#/components/schemas/SettingsValues'
    SettingsUpdateData:
      type: object
      required:
        - item
      properties:
        item:
          $ref: '#/components/schemas/SettingsResource'
    AuditEventOutcome:
      type: string
      enum: ['success', 'failure']
    AuditEventSeverity:
      type: string
      enum: ['info', 'warning', 'critical']
    AuditEventListItem:
      type: object
      required:
        - eventId
        - occurredAt
        - category
        - action
        - outcome
        - severity
        - scopeAccountId
        - scopeDeviceId
      properties:
        eventId:
          type: string
        occurredAt:
          type: string
          format: date-time
        category:
          type: string
        action:
          type: string
        outcome:
          $ref: '#/components/schemas/AuditEventOutcome'
        severity:
          $ref: '#/components/schemas/AuditEventSeverity'
        scopeAccountId:
          type: string
        scopeDeviceId:
          type: string
        entityType:
          type: string
        entityId:
          type: string
        sessionId:
          type: string
        requestId:
          type: string
        correlationId:
          type: string
        relatedRecordId:
          type: string
    AuditEventDetailItem:
      allOf:
        - $ref: '#/components/schemas/AuditEventListItem'
        - type: object
          required:
            - metadata
            - retentionUntilAt
          properties:
            metadata:
              type: object
              additionalProperties: true
            retentionUntilAt:
              type: string
              format: date-time
    AuditEventPagination:
      type: object
      required:
        - page
        - pageSize
        - total
        - hasMore
      properties:
        page:
          type: integer
          minimum: 1
        pageSize:
          type: integer
          minimum: 1
        total:
          type: integer
          minimum: 0
        hasMore:
          type: boolean
    AuditEventListData:
      type: object
      required:
        - items
        - pagination
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AuditEventListItem'
        pagination:
          $ref: '#/components/schemas/AuditEventPagination'
    AuditEventDetailData:
      type: object
      required:
        - item
      properties:
        item:
          $ref: '#/components/schemas/AuditEventDetailItem'
    SshFolder:
      type: object
      required:
        - id
        - name
        - iconKey
        - colorKey
        - createdAt
        - updatedAt
      properties:
        id:
          type: string
        name:
          type: string
        iconKey:
          type: string
          minLength: 1
          maxLength: 64
        colorKey:
          $ref: '#/components/schemas/SshVisualColorKey'
        note:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    SshTag:
      type: object
      required:
        - id
        - name
        - createdAt
        - updatedAt
      properties:
        id:
          type: string
        name:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    SshLoginAudit:
      type: object
      required:
        - id
        - attemptedAt
        - result
        - commandCount
      properties:
        id:
          type: string
        attemptedAt:
          type: string
          format: date-time
        result:
          type: string
          enum: ['success', 'failed']
        failureReason:
          type: string
        clientIp:
          type: string
        sessionId:
          type: string
        sessionStartedAt:
          type: string
          format: date-time
        sessionEndedAt:
          type: string
          format: date-time
        commandCount:
          type: integer
          minimum: 0
    SshServerListItem:
      type: object
      required:
        - id
        - name
        - iconKey
        - colorKey
        - host
        - port
        - username
        - keychainId
        - authType
        - strictHostKey
        - enableSshCompression
        - remoteEnhancementsEnabled
        - disableCharacterWidthCompatibilityMode
        - terminalClipboardAccess
        - mcpCommandPolicy
        - proxyMode
        - hasPassword
        - hasPrivateKey
        - createdAt
        - updatedAt
      properties:
        id:
          type: string
        name:
          type: string
        iconKey:
          type: string
          minLength: 1
          maxLength: 64
        colorKey:
          $ref: '#/components/schemas/SshVisualColorKey'
        host:
          type: string
        port:
          type: integer
          minimum: 1
          maximum: 65535
        username:
          type: string
        keychainId:
          type: string
        authType:
          $ref: '#/components/schemas/SshAuthType'
        strictHostKey:
          type: boolean
        enableSshCompression:
          type: boolean
        remoteEnhancementsEnabled:
          type: boolean
        disableCharacterWidthCompatibilityMode:
          type: boolean
        terminalClipboardAccess:
          $ref: '#/components/schemas/TerminalClipboardAccess'
        mcpCommandPolicy:
          $ref: '#/components/schemas/McpServerCommandPolicy'
        proxyMode:
          $ref: '#/components/schemas/SshServerProxyMode'
        proxyUrl:
          type: string
          maxLength: 2048
        hasPassword:
          type: boolean
        hasPrivateKey:
          type: boolean
        note:
          type: string
        folder:
          $ref: '#/components/schemas/SshFolder'
        tags:
          type: array
          items:
            $ref: '#/components/schemas/SshTag'
        systemHostname:
          type: string
        systemOs:
          type: string
        systemArch:
          type: string
        systemKernel:
          type: string
        lastSystemSyncAt:
          type: string
          format: date-time
        lastLoginAudit:
          $ref: '#/components/schemas/SshLoginAudit'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    SshServerCreateRequest:
      type: object
      required:
        - name
        - host
        - port
        - username
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
        host:
          type: string
          minLength: 1
          maxLength: 255
        port:
          type: integer
          minimum: 1
          maximum: 65535
        username:
          type: string
          minLength: 1
          maxLength: 120
        keychainId:
          type: string
        authType:
          $ref: '#/components/schemas/SshAuthType'
        strictHostKey:
          type: boolean
        enableSshCompression:
          type: boolean
        remoteEnhancementsEnabled:
          type: boolean
        disableCharacterWidthCompatibilityMode:
          type: boolean
        terminalClipboardAccess:
          $ref: '#/components/schemas/TerminalClipboardAccess'
        mcpCommandPolicy:
          $ref: '#/components/schemas/McpServerCommandPolicy'
        proxyMode:
          $ref: '#/components/schemas/SshServerProxyMode'
        proxyUrl:
          type: string
          maxLength: 2048
        iconKey:
          type: string
          minLength: 1
          maxLength: 64
        colorKey:
          $ref: '#/components/schemas/SshVisualColorKey'
        password:
          type: string
          minLength: 1
        privateKey:
          type: string
          minLength: 1
        privateKeyPassphrase:
          type: string
          minLength: 1
        folderId:
          type: string
        tagIds:
          type: array
          items:
            type: string
        note:
          type: string
          maxLength: 3000
    SshFolderCreateRequest:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
        iconKey:
          type: string
          minLength: 1
          maxLength: 64
        colorKey:
          $ref: '#/components/schemas/SshVisualColorKey'
        note:
          type: string
          maxLength: 1000
    SshFolderUpdateRequest:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
        iconKey:
          type: string
          minLength: 1
          maxLength: 64
        colorKey:
          $ref: '#/components/schemas/SshVisualColorKey'
        note:
          type: string
          maxLength: 1000
    SshVisualColorKey:
      type: string
      enum: ['slate', 'blue', 'emerald', 'violet', 'amber', 'rose', 'cyan', 'indigo', 'teal', 'lime']
    SshServerProxyMode:
      type: string
      enum: ['default', 'off', 'custom']
    SshTagCreateRequest:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 64
    SshSessionCreateRequest:
      type: object
      required:
        - serverId
      properties:
        serverId:
          type: string
        cols:
          type: integer
          minimum: 20
          maximum: 400
          default: 120
        rows:
          type: integer
          minimum: 10
          maximum: 200
          default: 32
        term:
          type: string
          minLength: 2
          maxLength: 64
          default: xterm-256color
        connectTimeoutSec:
          type: integer
          minimum: 5
          maximum: 180
          default: 45
        strictHostKey:
          type: boolean
        enableSshCompression:
          type: boolean
        remoteEnhancementsEnabled:
          type: boolean
        systemProxyRules:
          type: string
          maxLength: 4096
    TerminalClipboardAccess:
      type: string
      enum: ['off', 'writeAskRead', 'readWrite', 'askAlways']
    McpCommandPolicy:
      type: string
      enum: ['off', 'ask', 'allowWithinConnection']
    McpServerCommandPolicy:
      type: string
      enum: ['default', 'off', 'ask', 'allowWithinConnection']
    SshTrustFingerprintRequest:
      type: object
      required:
        - serverId
        - fingerprintSha256
      properties:
        serverId:
          type: string
        fingerprintSha256:
          type: string
          minLength: 1
          maxLength: 255
        algorithm:
          type: string
          enum: ['sha256']
          default: sha256
    SshHostVerificationRequiredData:
      type: object
      required:
        - serverId
        - host
        - port
        - algorithm
        - fingerprint
      properties:
        serverId:
          type: string
        host:
          type: string
        port:
          type: integer
        algorithm:
          type: string
          enum: ['sha256']
        fingerprint:
          type: string
    SshHostVerificationRequired:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - code
            - data
          properties:
            code:
              type: string
              enum: ['SSH_HOST_UNTRUSTED']
            success:
              type: boolean
              enum: [false]
            data:
              $ref: '#/components/schemas/SshHostVerificationRequiredData'
    SshServerListData:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SshServerListItem'
    SshFolderListData:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SshFolder'
    SshTagListData:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SshTag'
    SshServerCreateData:
      type: object
      required:
        - item
      properties:
        item:
          $ref: '#/components/schemas/SshServerListItem'
    SshServerCredentialsData:
      type: object
      required:
        - authType
      properties:
        authType:
          $ref: '#/components/schemas/SshAuthType'
        password:
          type: string
        privateKey:
          type: string
        privateKeyPassphrase:
          type: string
    SshKeychainListItem:
      type: object
      required:
        - id
        - name
        - iconKey
        - colorKey
        - authType
        - visibility
        - hasPassword
        - hasPrivateKey
        - tags
        - createdAt
        - updatedAt
      properties:
        id:
          type: string
        name:
          type: string
        iconKey:
          type: string
        colorKey:
          $ref: '#/components/schemas/SshVisualColorKey'
        authType:
          $ref: '#/components/schemas/SshAuthType'
        visibility:
          $ref: '#/components/schemas/SshKeychainVisibility'
        hasPassword:
          type: boolean
        hasPrivateKey:
          type: boolean
        note:
          type: string
        folder:
          $ref: '#/components/schemas/SshFolder'
        tags:
          type: array
          items:
            $ref: '#/components/schemas/SshTag'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    SshKeychainListData:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SshKeychainListItem'
    SshKeychainCreateRequest:
      type: object
      required:
        - name
        - authType
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
        iconKey:
          type: string
          minLength: 1
          maxLength: 64
        colorKey:
          $ref: '#/components/schemas/SshVisualColorKey'
        authType:
          $ref: '#/components/schemas/SshAuthType'
        visibility:
          $ref: '#/components/schemas/SshKeychainVisibility'
        folderId:
          type: string
          minLength: 1
        tagIds:
          type: array
          items:
            type: string
            minLength: 1
        password:
          type: string
          minLength: 1
        privateKey:
          type: string
          minLength: 1
        privateKeyPassphrase:
          type: string
          minLength: 1
        note:
          type: string
          maxLength: 3000
    SshKeychainUpdateRequest:
      allOf:
        - $ref: '#/components/schemas/SshKeychainCreateRequest'
    SshKeychainCreateData:
      type: object
      required:
        - item
      properties:
        item:
          $ref: '#/components/schemas/SshKeychainListItem'
    SshKeychainCredentialsData:
      type: object
      required:
        - authType
      properties:
        authType:
          $ref: '#/components/schemas/SshAuthType'
        password:
          type: string
        privateKey:
          type: string
        privateKeyPassphrase:
          type: string
    SshFolderCreateData:
      type: object
      required:
        - item
      properties:
        item:
          $ref: '#/components/schemas/SshFolder'
    SshTagCreateData:
      type: object
      required:
        - item
      properties:
        item:
          $ref: '#/components/schemas/SshTag'
    SshSessionCreateData:
      type: object
      required:
        - sessionId
        - serverId
        - websocketUrl
        - websocketToken
      properties:
        sessionId:
          type: string
        serverId:
          type: string
        websocketUrl:
          type: string
        websocketToken:
          type: string
    SshTrustFingerprintData:
      type: object
      required:
        - trusted
      properties:
        trusted:
          type: boolean
          enum: [true]
    PortForwardRuleType:
      type: string
      enum: ['local', 'remote', 'dynamic']
    PortForwardRuleStatus:
      type: string
      enum: ['stopped', 'running']
    PortForwardRuleRuntime:
      type: object
      required:
        - status
        - activeConnectionCount
      properties:
        status:
          $ref: '#/components/schemas/PortForwardRuleStatus'
        activeConnectionCount:
          type: integer
          minimum: 0
        boundHost:
          type: string
        boundPort:
          type: integer
          minimum: 1
          maximum: 65535
        startedAt:
          type: string
          format: date-time
        lastError:
          type: string
    PortForwardRuleListItem:
      type: object
      required:
        - id
        - name
        - type
        - serverId
        - runtime
        - createdAt
        - updatedAt
      properties:
        id:
          type: string
        name:
          type: string
          minLength: 1
          maxLength: 120
        type:
          $ref: '#/components/schemas/PortForwardRuleType'
        serverId:
          type: string
        serverName:
          type: string
        localBindHost:
          type: string
          maxLength: 255
        localBindPort:
          type: integer
          minimum: 1
          maximum: 65535
        remoteBindHost:
          type: string
          maxLength: 255
        remoteBindPort:
          type: integer
          minimum: 1
          maximum: 65535
        targetHost:
          type: string
          maxLength: 255
        targetPort:
          type: integer
          minimum: 1
          maximum: 65535
        note:
          type: string
          maxLength: 3000
        lastStartedAt:
          type: string
          format: date-time
        lastStoppedAt:
          type: string
          format: date-time
        lastFailureMessage:
          type: string
        runtime:
          $ref: '#/components/schemas/PortForwardRuleRuntime'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    PortForwardRuleCreateRequest:
      type: object
      required:
        - name
        - serverId
        - type
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
        serverId:
          type: string
        type:
          $ref: '#/components/schemas/PortForwardRuleType'
        localBindHost:
          type: string
          maxLength: 255
        localBindPort:
          type: integer
          minimum: 1
          maximum: 65535
        remoteBindHost:
          type: string
          maxLength: 255
        remoteBindPort:
          type: integer
          minimum: 1
          maximum: 65535
        targetHost:
          type: string
          maxLength: 255
        targetPort:
          type: integer
          minimum: 1
          maximum: 65535
        note:
          type: string
          maxLength: 3000
    PortForwardRuleUpdateRequest:
      allOf:
        - $ref: '#/components/schemas/PortForwardRuleCreateRequest'
    PortForwardRuleListData:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PortForwardRuleListItem'
    PortForwardRuleData:
      type: object
      required:
        - item
      properties:
        item:
          $ref: '#/components/schemas/PortForwardRuleListItem'
    PortForwardRuleStartRequest:
      type: object
      properties:
        systemProxyRules:
          type: string
          maxLength: 4096
    SftpSessionCreateRequest:
      type: object
      required:
        - serverId
      properties:
        serverId:
          type: string
        initialPath:
          type: string
        connectTimeoutSec:
          type: integer
          minimum: 5
          maximum: 180
        strictHostKey:
          type: boolean
        systemProxyRules:
          type: string
          maxLength: 4096
    SftpPathRequest:
      type: object
      required:
        - path
      properties:
        path:
          type: string
          minLength: 1
    SftpCreateDirectoryRequest:
      allOf:
        - $ref: '#/components/schemas/SftpPathRequest'
    SftpCreateFileRequest:
      allOf:
        - $ref: '#/components/schemas/SftpPathRequest'
    SftpDownloadFileRequest:
      type: object
      required:
        - path
        - localPath
      properties:
        path:
          type: string
          minLength: 1
        localPath:
          type: string
          minLength: 1
        transferId:
          type: string
          format: uuid
          description: Renderer-generated identifier used to query byte progress while the transfer is active.
    SftpUploadFileRequest:
      type: object
      required:
        - path
        - localPath
      properties:
        path:
          type: string
          minLength: 1
        localPath:
          type: string
          minLength: 1
        transferId:
          type: string
          format: uuid
          description: Renderer-generated identifier used to query byte progress while the transfer is active.
        expectedSize:
          type: integer
          format: int64
          minimum: 0
          description: Remote file size captured when an existing file was opened. Must be paired with expectedModifiedAt.
        expectedModifiedAt:
          type: string
          format: date-time
          description: Remote modified time captured when an existing file was opened. Must be paired with expectedSize.
        overwrite:
          type: boolean
          description: Explicitly replace an existing remote file after renderer confirmation.
    SftpWriteFileRequest:
      type: object
      required:
        - path
        - content
        - expectedSize
        - expectedModifiedAt
      properties:
        path:
          type: string
          minLength: 1
        content:
          type: string
          maxLength: 16777216
        expectedSize:
          type: integer
          format: int64
          minimum: 0
        expectedModifiedAt:
          type: string
          format: date-time
        overwrite:
          type: boolean
          description: Explicitly replace the remote file even when its size or modified time no longer matches the preview snapshot.
    SftpRenameRequest:
      type: object
      required:
        - sourcePath
        - targetPath
      properties:
        sourcePath:
          type: string
          minLength: 1
        targetPath:
          type: string
          minLength: 1
    SftpCopyRequest:
      type: object
      required:
        - sourcePath
        - targetPath
      properties:
        sourcePath:
          type: string
          minLength: 1
        targetPath:
          type: string
          minLength: 1
    SftpDeleteRequest:
      allOf:
        - $ref: '#/components/schemas/SftpPathRequest'
        - type: object
          properties:
            recursive:
              type: boolean
              default: true
    SftpBatchOperationType:
      type: string
      enum: ['copy', 'move', 'delete', 'link']
    SftpBatchOperationItemRequest:
      type: object
      required:
        - path
        - type
      properties:
        path:
          type: string
          minLength: 1
        type:
          $ref: '#/components/schemas/SftpEntryType'
    SftpBatchOperationRequest:
      type: object
      required:
        - operation
        - entries
      properties:
        operation:
          $ref: '#/components/schemas/SftpBatchOperationType'
        entries:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/SftpBatchOperationItemRequest'
        targetDirectoryPath:
          type: string
          minLength: 1
          description: Required when operation is copy, move, or link.
    SftpTaskOperationType:
      type: string
      enum: ['create-file', 'create-directory', 'rename', 'upload', 'download', 'batch']
    SftpTaskState:
      type: string
      enum: ['queued', 'running', 'succeeded', 'failed', 'cancelled']
    SftpTaskErrorCode:
      type: string
      enum:
        [
          'SFTP_TASK_DEADLINE_EXCEEDED',
          'SFTP_OPERATION_TIMEOUT',
          'SFTP_TRANSPORT_CLOSED',
          'SFTP_SESSION_NOT_FOUND',
          'SFTP_OPERATION_FAILED',
          'SFTP_UPLOAD_CONFLICT',
          'SFTP_VALIDATION_FAILED',
        ]
    SftpTaskTransferReference:
      type: object
      required:
        - transferId
      properties:
        transferId:
          type: string
          format: uuid
    SftpTaskCreateFileDescriptor:
      type: object
      required:
        - operation
        - payload
      properties:
        operation:
          type: string
          enum: ['create-file']
        payload:
          $ref: '#/components/schemas/SftpCreateFileRequest'
    SftpTaskCreateDirectoryDescriptor:
      type: object
      required:
        - operation
        - payload
      properties:
        operation:
          type: string
          enum: ['create-directory']
        payload:
          $ref: '#/components/schemas/SftpCreateDirectoryRequest'
    SftpTaskRenameDescriptor:
      type: object
      required:
        - operation
        - payload
      properties:
        operation:
          type: string
          enum: ['rename']
        payload:
          $ref: '#/components/schemas/SftpRenameRequest'
    SftpTaskUploadDescriptor:
      type: object
      required:
        - operation
        - payload
      properties:
        operation:
          type: string
          enum: ['upload']
        payload:
          allOf:
            - $ref: '#/components/schemas/SftpUploadFileRequest'
            - $ref: '#/components/schemas/SftpTaskTransferReference'
    SftpTaskDownloadDescriptor:
      type: object
      required:
        - operation
        - payload
      properties:
        operation:
          type: string
          enum: ['download']
        payload:
          allOf:
            - $ref: '#/components/schemas/SftpDownloadFileRequest'
            - $ref: '#/components/schemas/SftpTaskTransferReference'
    SftpTaskBatchDescriptor:
      type: object
      required:
        - operation
        - payload
      properties:
        operation:
          type: string
          enum: ['batch']
        payload:
          $ref: '#/components/schemas/SftpBatchOperationRequest'
    SftpTaskStartRequest:
      description: Write-file is intentionally excluded because asynchronous task records accept references and paths, not inline file content.
      oneOf:
        - $ref: '#/components/schemas/SftpTaskCreateFileDescriptor'
        - $ref: '#/components/schemas/SftpTaskCreateDirectoryDescriptor'
        - $ref: '#/components/schemas/SftpTaskRenameDescriptor'
        - $ref: '#/components/schemas/SftpTaskUploadDescriptor'
        - $ref: '#/components/schemas/SftpTaskDownloadDescriptor'
        - $ref: '#/components/schemas/SftpTaskBatchDescriptor'
      discriminator:
        propertyName: operation
        mapping:
          create-file: '#/components/schemas/SftpTaskCreateFileDescriptor'
          create-directory: '#/components/schemas/SftpTaskCreateDirectoryDescriptor'
          rename: '#/components/schemas/SftpTaskRenameDescriptor'
          upload: '#/components/schemas/SftpTaskUploadDescriptor'
          download: '#/components/schemas/SftpTaskDownloadDescriptor'
          batch: '#/components/schemas/SftpTaskBatchDescriptor'
    SftpBatchOperationItemStatus:
      type: string
      enum: ['success', 'failed', 'skipped']
    SftpBatchOperationItemResult:
      type: object
      required:
        - path
        - type
        - status
      properties:
        path:
          type: string
        type:
          $ref: '#/components/schemas/SftpEntryType'
        targetPath:
          type: string
        status:
          $ref: '#/components/schemas/SftpBatchOperationItemStatus'
        message:
          type: string
    SftpArchiveFormat:
      type: string
      enum: ['tar', 'tar-gzip', 'zip', 'tar-xz', 'tar-bzip2', '7z']
    SftpArchiveCompressionLevel:
      type: string
      enum: ['store', 'fast', 'standard', 'maximum']
    SftpArchiveDestinationMode:
      type: string
      enum: ['smart', 'current-directory', 'archive-name-directory']
    SftpArchiveConflictResolution:
      type: string
      enum: ['overwrite', 'keep-both', 'cancel']
    SftpArchiveOperationType:
      type: string
      enum: ['compress', 'extract']
    SftpArchiveOperationState:
      type: string
      enum: ['running', 'awaiting-conflict', 'succeeded', 'failed', 'cancelled']
    SftpArchiveOperationStage:
      type: string
      enum:
        [
          'preparing',
          'compressing',
          'extracting',
          'verifying',
          'awaiting-conflict',
          'committing',
          'cleaning',
          'completed',
        ]
    SftpArchiveErrorCode:
      type: string
      enum:
        - 'SFTP_ARCHIVE_UNSUPPORTED'
        - 'SFTP_ARCHIVE_BUSY'
        - 'SFTP_ARCHIVE_TARGET_EXISTS'
        - 'SFTP_ARCHIVE_UNSAFE_ENTRY'
        - 'SFTP_ARCHIVE_OPERATION_NOT_FOUND'
        - 'SFTP_ARCHIVE_OPERATION_FAILED'
        - 'SFTP_ARCHIVE_TIMEOUT'
        - 'SFTP_ARCHIVE_CANCEL_FAILED'
        - 'SFTP_VALIDATION_FAILED'
    SftpArchiveCompressRequest:
      type: object
      required:
        - type
        - sourcePaths
        - targetDirectoryPath
        - archiveName
        - format
        - compressionLevel
      properties:
        type:
          type: string
          enum: ['compress']
        sourcePaths:
          type: array
          minItems: 1
          items:
            type: string
            minLength: 1
        targetDirectoryPath:
          type: string
          minLength: 1
        archiveName:
          type: string
          minLength: 1
        format:
          $ref: '#/components/schemas/SftpArchiveFormat'
        compressionLevel:
          $ref: '#/components/schemas/SftpArchiveCompressionLevel'
    SftpArchiveExtractRequest:
      type: object
      required:
        - type
        - archivePath
        - targetDirectoryPath
        - destinationMode
      properties:
        type:
          type: string
          enum: ['extract']
        archivePath:
          type: string
          minLength: 1
          description: Absolute POSIX path of a regular archive file in the active SFTP session.
        targetDirectoryPath:
          type: string
          minLength: 1
          description: Absolute POSIX destination directory in the active SFTP session. Missing path segments are created; the remote root is rejected.
        destinationMode:
          $ref: '#/components/schemas/SftpArchiveDestinationMode'
    SftpArchiveOperationRequest:
      oneOf:
        - $ref: '#/components/schemas/SftpArchiveCompressRequest'
        - $ref: '#/components/schemas/SftpArchiveExtractRequest'
      discriminator:
        propertyName: type
        mapping:
          compress: '#/components/schemas/SftpArchiveCompressRequest'
          extract: '#/components/schemas/SftpArchiveExtractRequest'
    SftpArchiveConflictResolutionRequest:
      type: object
      required:
        - resolution
      properties:
        resolution:
          $ref: '#/components/schemas/SftpArchiveConflictResolution'
    SftpArchiveConflict:
      type: object
      required:
        - path
        - targetPath
        - type
      properties:
        path:
          type: string
        targetPath:
          type: string
        type:
          $ref: '#/components/schemas/SftpEntryType'
    SftpEntryType:
      type: string
      enum: ['directory', 'file', 'symlink', 'other']
    SftpEntry:
      type: object
      required:
        - name
        - path
        - type
        - size
        - mode
        - permissions
        - permissionOctal
        - uid
        - gid
        - modifiedAt
        - accessedAt
        - extension
        - shellEscapedPath
        - isHidden
      properties:
        name:
          type: string
        path:
          type: string
        parentPath:
          type: string
        type:
          $ref: '#/components/schemas/SftpEntryType'
        size:
          type: integer
          format: int64
        mode:
          type: integer
        permissions:
          type: string
        permissionOctal:
          type: string
          pattern: '^[0-7]{4}$'
        uid:
          type: integer
        gid:
          type: integer
        modifiedAt:
          type: string
          format: date-time
        accessedAt:
          type: string
          format: date-time
        extension:
          type: string
        shellEscapedPath:
          type: string
        isHidden:
          type: boolean
        longname:
          type: string
        symlinkTarget:
          $ref: '#/components/schemas/SftpSymlinkTarget'
    SftpSymlinkTargetStatus:
      type: string
      enum: ['exists', 'broken', 'permission-denied', 'unknown']
    SftpSymlinkTarget:
      type: object
      required:
        - status
      properties:
        status:
          $ref: '#/components/schemas/SftpSymlinkTargetStatus'
        path:
          type: string
          description: Raw target returned by readlink.
        resolvedPath:
          type: string
          description: Absolute target path resolved from the link parent when possible.
        isAbsolute:
          type: boolean
        type:
          $ref: '#/components/schemas/SftpEntryType'
        size:
          type: integer
          format: int64
        mode:
          type: integer
        permissions:
          type: string
        permissionOctal:
          type: string
          pattern: '^[0-7]{4}$'
        modifiedAt:
          type: string
          format: date-time
        accessedAt:
          type: string
          format: date-time
        message:
          type: string
    SftpEntryDetailsRequest:
      type: object
      required:
        - paths
      properties:
        paths:
          type: array
          minItems: 1
          maxItems: 200
          items:
            type: string
            minLength: 1
    SftpEntryDetailsItemStatus:
      type: string
      enum: ['success', 'failed']
    SftpEntryDetailsItem:
      type: object
      required:
        - path
        - status
      properties:
        path:
          type: string
        status:
          $ref: '#/components/schemas/SftpEntryDetailsItemStatus'
        entry:
          $ref: '#/components/schemas/SftpEntry'
        message:
          type: string
    SftpSessionCreateData:
      type: object
      required:
        - sessionId
        - serverId
        - initialPath
        - currentPath
        - readOnly
      properties:
        sessionId:
          type: string
        serverId:
          type: string
        initialPath:
          type: string
        currentPath:
          type: string
        readOnly:
          type: boolean
          enum: [false]
    SftpDirectoryListData:
      type: object
      required:
        - sessionId
        - path
        - entries
      properties:
        sessionId:
          type: string
        path:
          type: string
        parentPath:
          type: string
        entries:
          type: array
          items:
            $ref: '#/components/schemas/SftpEntry'
    SftpEntryDetailsData:
      type: object
      required:
        - sessionId
        - requestedCount
        - entries
      properties:
        sessionId:
          type: string
        requestedCount:
          type: integer
          minimum: 0
        entries:
          type: array
          items:
            $ref: '#/components/schemas/SftpEntryDetailsItem'
    SftpOperationData:
      type: object
      required:
        - sessionId
        - path
      properties:
        sessionId:
          type: string
        path:
          type: string
        targetPath:
          type: string
        size:
          type: integer
          format: int64
        modifiedAt:
          type: string
          format: date-time
    SftpBatchOperationData:
      type: object
      required:
        - sessionId
        - operation
        - totalCount
        - completedCount
        - failedCount
        - skippedCount
        - stoppedOnFailure
        - results
      properties:
        sessionId:
          type: string
        operation:
          $ref: '#/components/schemas/SftpBatchOperationType'
        totalCount:
          type: integer
          minimum: 0
        completedCount:
          type: integer
          minimum: 0
        failedCount:
          type: integer
          minimum: 0
        skippedCount:
          type: integer
          minimum: 0
        stoppedOnFailure:
          type: boolean
        results:
          type: array
          items:
            $ref: '#/components/schemas/SftpBatchOperationItemResult'
    SftpArchiveCapabilitiesData:
      type: object
      required:
        - sessionId
        - canExec
        - createFormats
        - extractFormats
      properties:
        sessionId:
          type: string
        canExec:
          type: boolean
        createFormats:
          type: array
          items:
            $ref: '#/components/schemas/SftpArchiveFormat'
        extractFormats:
          type: array
          items:
            $ref: '#/components/schemas/SftpArchiveFormat'
    SftpArchiveOperationData:
      type: object
      required:
        - sessionId
        - operationId
        - type
        - state
        - stage
        - cancelRequested
      properties:
        sessionId:
          type: string
        operationId:
          type: string
        type:
          $ref: '#/components/schemas/SftpArchiveOperationType'
        state:
          $ref: '#/components/schemas/SftpArchiveOperationState'
        stage:
          $ref: '#/components/schemas/SftpArchiveOperationStage'
        cancelRequested:
          type: boolean
        conflicts:
          type: array
          items:
            $ref: '#/components/schemas/SftpArchiveConflict'
        resultPaths:
          type: array
          items:
            type: string
        errorCode:
          $ref: '#/components/schemas/SftpArchiveErrorCode'
        errorMessage:
          type: string
    SftpReadFileData:
      type: object
      required:
        - sessionId
        - path
        - encoding
        - content
        - size
        - truncated
      properties:
        sessionId:
          type: string
        path:
          type: string
        encoding:
          type: string
          enum: ['utf8']
        content:
          type: string
        size:
          type: integer
          format: int64
        truncated:
          type: boolean
    SftpDownloadFileData:
      type: object
      required:
        - sessionId
        - path
        - localPath
        - size
      properties:
        sessionId:
          type: string
        path:
          type: string
        localPath:
          type: string
        size:
          type: integer
          format: int64
    SftpTaskOperationResult:
      type: object
      required:
        - type
        - data
      properties:
        type:
          type: string
          enum: ['operation']
        data:
          $ref: '#/components/schemas/SftpOperationData'
    SftpTaskDownloadResult:
      type: object
      required:
        - type
        - data
      properties:
        type:
          type: string
          enum: ['download']
        data:
          $ref: '#/components/schemas/SftpDownloadFileData'
    SftpTaskBatchResult:
      type: object
      required:
        - type
        - data
      properties:
        type:
          type: string
          enum: ['batch']
        data:
          $ref: '#/components/schemas/SftpBatchOperationData'
    SftpTaskResult:
      oneOf:
        - $ref: '#/components/schemas/SftpTaskOperationResult'
        - $ref: '#/components/schemas/SftpTaskDownloadResult'
        - $ref: '#/components/schemas/SftpTaskBatchResult'
      discriminator:
        propertyName: type
        mapping:
          operation: '#/components/schemas/SftpTaskOperationResult'
          download: '#/components/schemas/SftpTaskDownloadResult'
          batch: '#/components/schemas/SftpTaskBatchResult'
    SftpTaskData:
      type: object
      required:
        - sessionId
        - taskId
        - operation
        - state
        - remotePaths
        - createdAt
        - deadlineAt
      properties:
        sessionId:
          type: string
        taskId:
          type: string
          format: uuid
        operation:
          $ref: '#/components/schemas/SftpTaskOperationType'
        state:
          $ref: '#/components/schemas/SftpTaskState'
        remotePaths:
          type: array
          minItems: 1
          items:
            type: string
            minLength: 1
        transferId:
          type: string
          format: uuid
        createdAt:
          type: string
          format: date-time
        deadlineAt:
          type: string
          format: date-time
        startedAt:
          type: string
          format: date-time
        finishedAt:
          type: string
          format: date-time
        result:
          $ref: '#/components/schemas/SftpTaskResult'
        errorCode:
          $ref: '#/components/schemas/SftpTaskErrorCode'
        errorMessage:
          type: string
        outcomeUnknown:
          type: boolean
    SftpTaskListData:
      type: object
      required:
        - sessionId
        - items
      properties:
        sessionId:
          type: string
        items:
          type: array
          items:
            $ref: '#/components/schemas/SftpTaskData'
    SftpTransferProgressData:
      type: object
      required:
        - transferId
        - direction
        - status
        - transferredBytes
        - totalBytes
        - bytesPerSecond
        - startedAt
        - updatedAt
      properties:
        transferId:
          type: string
          format: uuid
        direction:
          type: string
          enum: ['download', 'upload']
        status:
          type: string
          enum: ['running', 'completed', 'failed']
        transferredBytes:
          type: integer
          format: int64
          minimum: 0
        totalBytes:
          type: integer
          format: int64
          minimum: 0
        bytesPerSecond:
          type: number
          format: double
          minimum: 0
        startedAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        finishedAt:
          type: string
          format: date-time
        errorMessage:
          type: string
    LocalTerminalProfile:
      type: object
      required:
        - id
        - name
        - command
        - executablePath
        - args
      properties:
        id:
          type: string
        name:
          type: string
        command:
          type: string
        executablePath:
          type: string
        args:
          type: array
          items:
            type: string
    LocalTerminalProfileListData:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/LocalTerminalProfile'
    LocalTerminalSessionCreateRequest:
      type: object
      required:
        - profileId
      properties:
        profileId:
          type: string
        cols:
          type: integer
          minimum: 20
          maximum: 400
          default: 120
        rows:
          type: integer
          minimum: 10
          maximum: 200
          default: 32
        term:
          type: string
          minLength: 2
          maxLength: 64
          default: xterm-256color
        cwd:
          type: string
          minLength: 1
    LocalTerminalSessionCreateData:
      type: object
      required:
        - sessionId
        - profileId
        - websocketUrl
        - websocketToken
      properties:
        sessionId:
          type: string
        profileId:
          type: string
        websocketUrl:
          type: string
        websocketToken:
          type: string
    LocalTerminalProfileListSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['LOCAL_TERMINAL_LIST_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/LocalTerminalProfileListData'
    LocalTerminalSessionCreateSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['LOCAL_TERMINAL_SESSION_CREATE_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/LocalTerminalSessionCreateData'
    SettingsGetSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SETTINGS_GET_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SettingsGetData'
    SettingsUpdateSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SETTINGS_UPDATE_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SettingsUpdateData'
    AuditEventListSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['AUDIT_EVENT_LIST_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/AuditEventListData'
    AuditEventDetailSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['AUDIT_EVENT_DETAIL_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/AuditEventDetailData'
    SshServerListSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SSH_SERVER_LIST_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SshServerListData'
    SshServerCreateSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SSH_SERVER_CREATE_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SshServerCreateData'
    SshServerUpdateSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SSH_SERVER_UPDATE_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SshServerCreateData'
    SshServerCredentialsSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SSH_SERVER_CREDENTIALS_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SshServerCredentialsData'
    SshKeychainListSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SSH_KEYCHAIN_LIST_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SshKeychainListData'
    SshKeychainCreateSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SSH_KEYCHAIN_CREATE_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SshKeychainCreateData'
    SshKeychainUpdateSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SSH_KEYCHAIN_UPDATE_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SshKeychainCreateData'
    SshKeychainCredentialsSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SSH_KEYCHAIN_CREDENTIALS_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SshKeychainCredentialsData'
    SshFolderListSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SSH_FOLDER_LIST_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SshFolderListData'
    SshFolderCreateSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SSH_FOLDER_CREATE_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SshFolderCreateData'
    SshFolderUpdateSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SSH_FOLDER_UPDATE_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SshFolderCreateData'
    SshTagListSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SSH_TAG_LIST_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SshTagListData'
    SshTagCreateSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SSH_TAG_CREATE_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SshTagCreateData'
    SshSessionCreateSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SSH_SESSION_CREATE_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SshSessionCreateData'
    SshTrustFingerprintSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SSH_TRUST_FINGERPRINT_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SshTrustFingerprintData'
    PortForwardRuleListSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['PORT_FORWARD_RULE_LIST_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/PortForwardRuleListData'
    PortForwardRuleCreateSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['PORT_FORWARD_RULE_CREATE_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/PortForwardRuleData'
    PortForwardRuleUpdateSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['PORT_FORWARD_RULE_UPDATE_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/PortForwardRuleData'
    PortForwardRuleStartSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['PORT_FORWARD_RULE_START_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/PortForwardRuleData'
    PortForwardRuleStopSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['PORT_FORWARD_RULE_STOP_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/PortForwardRuleData'
    SftpSessionCreateSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SFTP_SESSION_CREATE_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SftpSessionCreateData'
    SftpDirectoryListSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SFTP_DIRECTORY_LIST_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SftpDirectoryListData'
    SftpEntryDetailsSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SFTP_ENTRY_DETAILS_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SftpEntryDetailsData'
    SftpOperationSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SFTP_OPERATION_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SftpOperationData'
    SftpBatchOperationSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SFTP_OPERATION_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SftpBatchOperationData'
    SftpTaskAcceptedSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SFTP_TASK_ACCEPTED']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SftpTaskData'
    SftpTaskStatusSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SFTP_TASK_STATUS_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SftpTaskData'
    SftpTaskListSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SFTP_TASK_LIST_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SftpTaskListData'
    SftpArchiveCapabilitiesSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SFTP_ARCHIVE_CAPABILITIES_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SftpArchiveCapabilitiesData'
    SftpArchiveOperationAcceptedSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SFTP_ARCHIVE_OPERATION_ACCEPTED']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SftpArchiveOperationData'
    SftpArchiveOperationStatusSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SFTP_ARCHIVE_OPERATION_STATUS_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SftpArchiveOperationData'
    SftpReadFileSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SFTP_FILE_READ_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SftpReadFileData'
    SftpDownloadFileSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SFTP_OPERATION_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SftpDownloadFileData'
    SftpTransferProgressSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['SFTP_TRANSFER_PROGRESS_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/SftpTransferProgressData'
    McpClientInfo:
      type: object
      required:
        - name
        - version
      properties:
        name:
          type: string
        version:
          type: string
    McpStatusData:
      type: object
      required:
        - enabled
        - tokenConfigured
        - activeClientCount
        - activeConnectionCount
        - pendingApprovalCount
      properties:
        enabled:
          type: boolean
        tokenConfigured:
          type: boolean
        discoveryFilePath:
          type: string
        bridgeLauncherPath:
          type: string
        activeClientCount:
          type: integer
          minimum: 0
        activeConnectionCount:
          type: integer
          minimum: 0
        pendingApprovalCount:
          type: integer
          minimum: 0
    McpStatusGetSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['MCP_STATUS_GET_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/McpStatusData'
    McpPairingTokenData:
      type: object
      required:
        - token
        - createdAt
      properties:
        token:
          type: string
          description: Plaintext pairing token; only returned once at rotation time.
        createdAt:
          type: string
          format: date-time
    McpPairingTokenRotateSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['MCP_PAIRING_TOKEN_ROTATE_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/McpPairingTokenData'
    McpClientSession:
      type: object
      required:
        - mcpSessionId
        - client
        - startedAt
        - lastActivityAt
      properties:
        mcpSessionId:
          type: string
        client:
          $ref: '#/components/schemas/McpClientInfo'
        startedAt:
          type: string
          format: date-time
        lastActivityAt:
          type: string
          format: date-time
    McpClientListData:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/McpClientSession'
    McpClientListSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['MCP_CLIENT_LIST_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/McpClientListData'
    McpConnectionSummary:
      type: object
      required:
        - connectionId
        - serverId
        - serverName
        - host
        - port
        - username
        - client
        - openedAt
        - lastUsedAt
        - commandCount
        - commandsPreApproved
      properties:
        connectionId:
          type: string
        serverId:
          type: string
        serverName:
          type: string
        host:
          type: string
        port:
          type: integer
        username:
          type: string
        client:
          $ref: '#/components/schemas/McpClientInfo'
        openedAt:
          type: string
          format: date-time
        lastUsedAt:
          type: string
          format: date-time
        commandCount:
          type: integer
          minimum: 0
        commandsPreApproved:
          type: boolean
    McpConnectionListData:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/McpConnectionSummary'
    McpConnectionListSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['MCP_CONNECTION_LIST_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/McpConnectionListData'
    McpApprovalKind:
      type: string
      enum: ['connection-open', 'command-execute']
    McpPendingApproval:
      type: object
      required:
        - approvalId
        - kind
        - createdAt
        - expiresAt
        - client
        - serverId
        - serverName
        - host
        - port
        - username
      properties:
        approvalId:
          type: string
        kind:
          $ref: '#/components/schemas/McpApprovalKind'
        createdAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        client:
          $ref: '#/components/schemas/McpClientInfo'
        serverId:
          type: string
        serverName:
          type: string
        host:
          type: string
        port:
          type: integer
        username:
          type: string
        reason:
          type: string
          maxLength: 500
        command:
          type: string
        connectionId:
          type: string
    McpApprovalListData:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/McpPendingApproval'
    McpApprovalListSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['MCP_APPROVAL_LIST_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/McpApprovalListData'
    McpApprovalDecisionRequest:
      type: object
      required:
        - decision
      properties:
        decision:
          type: string
          enum: ['approved', 'approvedForConnection', 'denied']
    McpApprovalResolveData:
      type: object
      required:
        - approvalId
        - decision
      properties:
        approvalId:
          type: string
        decision:
          type: string
          enum: ['approved', 'approvedForConnection', 'denied']
    McpApprovalResolveSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['MCP_APPROVAL_RESOLVE_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/McpApprovalResolveData'
    McpEventsChannelData:
      type: object
      required:
        - websocketUrl
        - websocketToken
      properties:
        websocketUrl:
          type: string
        websocketToken:
          type: string
    McpEventsChannelCreateSuccess:
      allOf:
        - $ref: '#/components/schemas/ApiMeta'
        - type: object
          required:
            - success
            - data
            - code
          properties:
            code:
              type: string
              enum: ['MCP_EVENTS_CHANNEL_CREATE_OK']
            success:
              type: boolean
              enum: [true]
            data:
              $ref: '#/components/schemas/McpEventsChannelData'
