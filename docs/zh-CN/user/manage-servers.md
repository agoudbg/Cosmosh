# 服务器管理

## 创建服务器配置

1. 打开服务器创建入口。
2. 填写主机、端口、用户名。
3. 选择认证方式（密码或私钥）。
4. 保存后执行一次快速连接验证。

## 默认备注模板

- 在“设置 > 高级 > 默认服务器备注模板”中配置的内容，会在新建服务器配置时预填到备注字段。
- 该模板仅作用于新建条目，不会覆盖已有服务器备注。

## 日常整理方式

1. 使用统一命名（例如 `env-region-role`）。
2. 按 `prod`、`staging`、`dev` 等环境分组。
3. 对高风险与低风险主机做明显区分。
4. 刚修改过的主机可临时放入复查分组。

### 标签行为说明

- 在首页查看某个文件夹时，标签筛选仍会显示，且仅展示该文件夹内实际使用的标签。
- 未被任何服务器使用的自定义 SSH 标签会在服务器或标签更新后自动清理。
- 保留标签 `favorite` 不会被自动删除。

## 更新与删除

1. 主机信息或凭据变更后立即更新条目。
2. 编辑后执行一次快速连接验证。
3. 清理过期条目，减少误连风险。
4. 删除生产条目前请再次确认。

## 截图占位

1. 服务器列表与环境分组。
2. 编辑服务器配置弹窗。
3. 删除服务器确认弹窗。
